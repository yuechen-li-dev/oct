package IfContracts

fn Select(flag: Bool, thenValue: Int, elseValue: Int) -> Int {
    return if flag { thenValue } else { elseValue }
}

fn Bucket(x: Int) -> Int {
    return if x > 0 { 1 } else { 0 }
}

fn LazyElseGuard() -> Int {
    return if true { 7 } else { 1 / 0 }
}
