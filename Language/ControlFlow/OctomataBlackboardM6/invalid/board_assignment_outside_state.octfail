expect error: "function Main: field assignment (`x.field = ...`) is only valid for `board.field` inside flow state bodies; for records, use immutable update (`x = x with { Field: value }`)"

package MainInvalid

record Board {
    FaultLatched: Bool
}

fn Main() -> Int {
    let board = Board { FaultLatched: false }
    board.FaultLatched = true
    return 0
}
