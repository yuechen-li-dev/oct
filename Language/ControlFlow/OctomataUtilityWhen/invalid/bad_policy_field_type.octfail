expect error: "function BadPolicyType: let next: utility when policy hysteresis must be Int"

package MainInvalid

flow BadPolicyType(flag: Bool) -> Int {
    state S {
        let next = when policy {
            hysteresis: true
            min_commit: 1
        } {
            case 1 when flag score 1
            else 0
        }
        return next
    }
}

fn Main() -> Int {
    return 0
}
