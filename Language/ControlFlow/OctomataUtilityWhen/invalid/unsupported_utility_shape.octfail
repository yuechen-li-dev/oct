expect error: "unsupported utility policy field 'unknown'"

package MainInvalid

flow BadShape(flag: Bool) -> Int {
    state S {
        let next = when policy {
            hysteresis: 1
            min_commit: 1
            unknown: 3
        } {
            case 1 when flag score 1
            else 0
        }
        return next
    }
}

fn Main() -> Int {
    return 0
}
