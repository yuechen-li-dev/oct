expect error: "function Main: when policy is only valid inside flow state bodies; outside flows use switch or when utility"

package MainInvalid

fn Main() -> Int {
    return when policy {
        hysteresis: 1
        min_commit: 1
    } {
        case 1 when true score 1
        else 0
    }
}
