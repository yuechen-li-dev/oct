package MainValid

flow ReturnBranch(flag: Bool) -> Int {
    state Start {
        when {
            case flag -> return 3
            else -> return 4
        }
    }
}

[Fact]
fn ReturnBranchCompiles() -> Void {
    Assert.Equal(1, 1, "when branch return expression is accepted")
}
