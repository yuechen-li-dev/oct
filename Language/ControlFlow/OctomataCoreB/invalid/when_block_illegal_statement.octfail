expect error: "function BadWhenBlock: a `when` action block may only contain flow-control actions (`remember`, `resume`, `goto`, `suspend`, `return`) and `board.field = ...` updates; move ordinary computation before the `when`, or use a normal block outside the flow guard"

package MainInvalid

flow BadWhenBlock(flag: Bool) -> Int {
    state Start {
        when {
            case flag -> {
                let x = 1
                return x
            }
            else -> return 0
        }
    }
}

fn Main() -> Int {
    return 0
}
