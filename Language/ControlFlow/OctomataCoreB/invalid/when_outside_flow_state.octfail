expect error: "function Main: guard when is only valid inside flow state bodies; outside flows use switch or when utility"

package MainInvalid

fn Main() -> Int {
    when {
        case true -> return 1
        else -> return 0
    }
}
