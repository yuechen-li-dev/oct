expect error: "flow BoardArrayUnsupported: board fields must be one of Bool, Int, Float, or String"

package MainInvalid

flow BoardArrayUnsupported() -> Void {
    board {
        Samples: Float[]
    }

    state Start {
        suspend
    }
}

fn Main() -> Int {
    return 0
}
