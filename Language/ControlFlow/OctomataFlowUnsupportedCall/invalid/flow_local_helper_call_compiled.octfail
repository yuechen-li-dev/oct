expect error: "compiled flow expressions do not yet support calling local/helper function `Code`"

package MainInvalid

fn Code(x: Int) -> Int {
    return x
}

flow Bad() -> Int {
    board {
        Value: Int
    }

    state Start {
        board.Value = Code(1)
        return board.Value
    }
}
