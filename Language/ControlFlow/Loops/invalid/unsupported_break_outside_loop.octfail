expect error: "`break` is not a loop-control keyword in Oct. Put the stop condition in the loop condition, or use Loop.Stop(state) with explicit Loop state helpers."

package Main

fn Main() -> Int {
    break
    return 0
}
