expect error: "function Main: var x: empty array literal `[]` requires array type context, got Int; use an explicit annotation like `var values: Int[] = []`"

package Main

fn Main() -> Int {
    var x: Int = []
    return x
}
