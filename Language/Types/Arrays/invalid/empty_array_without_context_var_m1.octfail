expect error: "function Main: var xs: empty array literal `[]` requires an expected array type; write `var values: Int[] = []` or assign it where an array type is already known"

package Main

fn Main() -> Int {
    var xs = []
    return Len(xs)
}
