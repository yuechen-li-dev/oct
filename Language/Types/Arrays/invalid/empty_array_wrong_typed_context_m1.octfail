expect error: "function Main: let x: empty array literal `[]` requires array type context, got Int; use an explicit annotation like `var values: Int[] = []`"

package Main

fn Main() -> Int {
    let x: Int = []
    return x
}
