expect error: "record table 'Measurements' column 'Stage': array element 0 expects String, got Int"

package MainInvalid

record table Measurements {Stage: String}

fn Main() -> Int {
    let table = Measurements {Stage:["A"]}
    let bad = table with {Stage:[1]}
    return Len(bad)
}
