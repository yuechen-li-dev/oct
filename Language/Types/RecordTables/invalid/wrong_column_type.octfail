expect error: "record table 'Measurements' column 'Latency': array element 0 expects Float, got String"

package MainInvalid

record table Measurements {Latency: Float}

fn Main() -> Int {
    let bad = Measurements {Latency:["slow"]}
    return Len(bad)
}
