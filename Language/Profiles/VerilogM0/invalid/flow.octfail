expect error: "Verilog profile does not support String values"

profile Verilog

flow Counter() -> Int {
    board { Label: String }
    state Start { return 1 }
}

fn Hardware() -> Int {
    return 0
}
