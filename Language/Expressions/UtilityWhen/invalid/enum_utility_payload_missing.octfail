expect error: "enum variant Decision.Run requires a payload"

package MainInvalid

enum Decision { Hold Run(Int) }

fn Judge(flag: Bool) -> Decision {
    return when utility Decision {
        case Decision.Run when flag score 10
        else Decision.Hold
    }
}
