expect error: "utility case condition must be Bool"

package MainInvalid

enum Decision {
    Hold
    Run
}

fn Main(flag: Int) -> Decision {
    return when utility Decision {
        case Decision.Run when flag score 10
        else Decision.Hold
    }
}
