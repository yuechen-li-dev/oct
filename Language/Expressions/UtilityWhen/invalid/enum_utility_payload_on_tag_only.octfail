expect error: "enum variant Decision.Run does not accept a payload"

package MainInvalid

enum Decision { Hold Run }

fn Judge(flag: Bool) -> Decision {
    return when utility Decision {
        case Decision.Run(1) when flag score 10
        else Decision.Hold
    }
}
