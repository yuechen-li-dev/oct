expect error: "utility else for Decision cannot return Other.Hold"

package MainInvalid

enum Decision {
    Hold
    Run
}

enum Other {
    Hold
}

fn Main(flag: Bool) -> Decision {
    return when utility Decision {
        case Decision.Run when flag score 10
        else Other.Hold
    }
}
