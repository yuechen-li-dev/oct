expect error: "Array.Where operates on arrays, not Vector<Float>; use Vector-specific APIs"

package Main

fn Main() -> Vector<Float>[] {
    let v = vector[1.0, 2.0]
    return Array.Where(v, [true])
}
