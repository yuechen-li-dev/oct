expect error: "function 'Matrix.tabulate' argument 3 expects function (Int, Int) -> T"

package Main

fn Bad(r: Int) -> Int {
    return r
}

fn Main() -> Matrix<Int> {
    return Matrix.tabulate(2, 2, Bad)
}
