expect error: "indexed tensor expressions must appear on both sides of '*'"

package Main

fn Main() -> Void {
    let i = Idx("i")
    let a = vector[1, 2]
    let b = vector[3, 4]
    let c = vector[5, 6]
    let _ = a[i] * b[i] * c[i]
}
