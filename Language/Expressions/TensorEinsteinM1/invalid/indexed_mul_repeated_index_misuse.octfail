expect error: "trace-style contraction '[i,i]' is not supported in M3; use Trace(...) for now"

package Main

fn Main() -> Void {
    let i = Idx("i")
    let j = Idx("j")

    let a = matrix[[1, 2] [3, 4]]
    let b = matrix[[5, 6] [7, 8]]

    let _ = a[i, i] * b[i, j]
}
