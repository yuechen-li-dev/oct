expect error: "function return types are not supported in M32"

package Main

fn Producer() -> fn(Int) -> Int {
    return Double
}

fn Double(x: Int) -> Int {
    return x * 2
}

fn Main() -> Int {
    return 0
}
