# Prometheus FFT Algorithm Lab

## Purpose

Prometheus FFT Algorithm Lab is a correctness-first Oct experiment track for FFT design and validation.

## Experiment package model

`manifest.oct` and `REPORT.md` live at the experiment root. Milestones (for example `M1`) are execution slices under this root and do not carry separate manifests.
