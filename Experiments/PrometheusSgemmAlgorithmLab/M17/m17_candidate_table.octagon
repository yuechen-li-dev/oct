[M17CandidateTable {
    Candidate: ["ScalarFP32", "Packed4FP32", "FP16StorageFP32Accum", "Packed4FP16StorageFP32Accum", "FP16StorageFP16Arithmetic"]
    UseDisposition: ["yes", "yes", "yes", "only experimental", "no"]
    Conditions: ["default baseline in all controller modes; use when strict FP32 correctness is required or capability gates block alternative layouts", "prefer when shape is medium/large and 4-alignment waste is within mode budget; strongest in aggressive mode under transfer pressure", "use when transfer pressure is high and storage quantization is acceptable; keep FP32 accumulation for stability", "experimental candidate for high transfer pressure when packed shape is favorable and quantization tolerance is explicit", "permit only in aggressive mode with explicit opt-in and loose numerical tolerance"]
    RejectionCases: ["none; this is mandatory fallback", "reject on tiny or skinny-heavy shapes with high non-multiple-of-4 waste, or when fallback path is missing", "reject for strict FP32 fidelity contracts, missing FP16 storage support, or uncertain tolerance", "reject on non-4-aligned small shapes, strict FP32 contracts, missing FP16 storage capability, or absent fallback", "reject for strict FP32 correctness, any missing FP16 capability, or non-opted workloads"]
    TransferVolumeUnits: [55296, 55296, 41472, 34560, 27648]
    PaddingWasteUnits: [0, 0, 0, 0, 0]
    ConversionCostUnits: [0, 0, 15360, 15360, 27648]
    LayoutComplexityScore: [1, 3, 4, 6, 5]
    SharedFootprintUnits: [20480, 23040, 10240, 11520, 10240]
    ArithmeticPrecisionRiskScore: [1, 1, 4, 6, 9]
    FallbackRequired: [false, true, true, true, true]
    ImplementationComplexityScore: [2, 4, 5, 7, 8]
    CorrectnessRiskScore: [1, 3, 5, 7, 9]
    EligibilityReason: ["always eligible", "eligible", "eligible", "eligible high-risk", "explicit FP16 arithmetic opt-in absent"]
}]
