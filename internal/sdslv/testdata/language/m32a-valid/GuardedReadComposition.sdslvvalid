fn GuardedReadComposition(
    Input: array<array<f32, 4u>, 4u>,
    M: u32,
    K: u32
) -> void {
    let Output: array<array<f32, 4u>, 4u>;
    tensor Output[i, j] = Sum[k](
        read Input[i, k] when i < M and k < K else 0.0
    );
    return;
}
