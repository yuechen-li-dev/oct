shader ResourceWrite {
    resources { Output: readwrite array<u32>; }
    stage compute [numthreads(1, 1, 1)] fn CS() -> void {
        let index: u32 = DispatchThreadID.x;
        Output[index] = 1u;
        return;
    }
}
