// Generated by GoOct SDSL-V M4 via VD-MIR. Do not edit by hand.
// namespace Prometheus.Audit

struct SgemmParams
{
    uint m;
    uint n;
    uint k;
};

[[vk::binding(0, 0)]] StructuredBuffer<float> A;
[[vk::binding(1, 0)]] StructuredBuffer<float> B;
[[vk::binding(2, 0)]] RWStructuredBuffer<float> C;

[[vk::push_constant]] ConstantBuffer<SgemmParams> params;

[numthreads(8, 8, 1)]
void SgemmSrt2AccumK_CS(uint3 DispatchThreadID : SV_DispatchThreadID)
{
    uint row = DispatchThreadID.x;
    uint col = DispatchThreadID.y;
    if ((row >= params.m))
    {
        return;
    }
    if ((col >= params.n))
    {
        return;
    }
    float even = 0.0;
    float odd = 0.0;
    uint base = (row * params.k);
    for (uint kk = 0u; kk < params.k; kk += 2u)
    {
        even = (even + (A[(base + kk)] * B[((kk * params.n) + col)]));
        uint next = (kk + 1u);
        if ((next < params.k))
        {
            odd = (odd + (A[(base + next)] * B[((next * params.n) + col)]));
        }
    }
    C[((row * params.n) + col)] = (even + odd);
    return;
}

