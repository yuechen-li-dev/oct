#include "reactor_vulkan.h"

/*
 * P12 M4 topology placeholder.
 *
 * This translation unit intentionally provides no runtime behavior.
 * It exists only to reserve source-file topology for a future fused-reduction
 * reactor family (softmax/layernorm/reduction/scan-style kernels,
 * family-specific resources/diagnostics/ABI if and when introduced).
 *
 * Non-claims:
 * - no public ABI symbols
 * - no capability reporting
 * - no probe/runtime behavior changes
 */
