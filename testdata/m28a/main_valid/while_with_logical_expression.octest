package MainValid

[Fact]
fn WhileWithLogicalExpression() -> Void {
    while true and not false {
        Assert.Equal(7, 7, "while with logical expression")
        return
    }
    Assert.True(false, "while should execute")
}
