package Main
import Analysis

fn Main() -> Int {
    let xs = [0.0, 1.0, 2.0, 3.0, 4.0]
    let ys = [0.0, 1.0, 4.0, 9.0, 16.0]
    let cs = Analysis.CumulativeSum(ys)!
    Print(Len(cs))
    Print(cs[0])
    return PlotLine(xs, ys, "analysis_output.png")
}
